* = code still in dev
:) = code works!

import pandas as pd

p2p_df = pd.read_csv('C:\\Users\\TWALTON\\Desktop\\procuretopay.csv',encoding='windows-1252')
hwsw_df = pd.read_excel('file_location.xlsx')

idx[idx.duplicated()].unique()  ## extract duplicates from index elements
df.columns = ['a', 'b'] ## Change column names
df.dtypes  ## view dtypes of all columns

## Add '00000' to requisition ID ## 
  * for req in p2p_df[0][0][1:]:
      print('00000'+str(req))

***** Merging multiple CSV files *****
import pandas as pd
import glob

files = glob.glob(r'2017-12-05\Aggregated\*.csv')   //folder which contains all the csv files
df = pd.concat([pd.read_csv(f, index_col=[0,1]) for f in files])

***** Merging multiple CSV files *****

##### Functions ##### (What do I need this thing to do)

#### def search_hwsw ####  search hwsw for requisition that does not have a po
  need to search multiple sheets
  need to skip the first 3 rows and any cell that does not start with 00000

  * df['Req to PO'] = np.where(df['Unnamed: 24'].isnull(), df['Unnamed: 13'], 'PO Issued')

#### def search_svc ####
    need to skip the first 3 rows and any cell that does not start with 00000
    create list of requisitions that have blank PO cells

    :) mr_svc['Req to PO'] = np.where(mr_svc['Unnamed: 24'].isnull(), mr_svc['Unnamed: 13'], 'PO Issued')

#### def search_p2p ####

    ### Need to match dtype of 'Requisition ID', 'Purchase Order ID', and 'Requisition to PO' columns
    * df.col = df.col.astype(int)
    * df.column_name = df.column_name.astype(str)
    
    ### match p2p req with search_svc req, returrn po#, line#, line $, purchaser, po date line = 1 then start loop over
    or
    ### slice col if p2p req matches search_svc
    df.loc[:, 'foo':'sat']  ## Slice columns
    df.loc[:, ['foo','bar','dat']]  ## Select specific columns
     * df = df.assign(COL3=np.where(df.COL1 == df.COL4,  df.loc[:, ['foo','bar','dat']], 'No PO Yet')) ## try to use the where attribute again ##
     *  DataFrame.isin(values)  ## returns boolean. Try a while not DataFrame.isin(values) return df.loc[:, ['foo','bar','dat']]
     
    ### append search_svc to p2p ###
      DONE
      
#### def save_hwsw ####
  save hwsw speadsheet

#### def save_hwsw ####
  save hwsw speadsheet  

#### def save_combine_po ####
  save hwsw speadsheet
  
  
  
  
